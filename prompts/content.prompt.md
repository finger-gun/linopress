Use the page-builder skill to create all pages, content, and navigation from the SiteSpec.

Every page should have substantive content appropriate to the site's purpose — no empty scaffolds.
Clean up any default WordPress content, set up navigation, and configure the front page.
{{originalPromptHint}}
{{blogHint}}
SiteSpec: {{siteSpec}}
