## 1. Tip Engine Foundations

- [x] 1.1 Add a prompt-tip analyzer utility module that maps prompt text to slot coverage (business type, audience, goals, visual style, key sections, CTA intent).
- [x] 1.2 Implement deterministic scoring that selects the highest-priority missing slot and returns exactly one active tip.
- [x] 1.3 Implement fallback behavior for empty/low-confidence analysis that returns a stable default onboarding tip.

## 2. Composer Integration

- [x] 2.1 Integrate analyzer evaluation into the prompt input flow in `PromptComposer` without introducing backend requests.
- [x] 2.2 Add a contextual tip presenter in the composer UI that displays one tip at a time with accessible semantics.
- [x] 2.3 Keep existing prompt action controls and submit loading behavior unchanged while adding tip rendering.

## 3. Stability and UX Guards

- [x] 3.1 Add debounced recomputation for tip updates to avoid per-keystroke flicker.
- [x] 3.2 Add immediate repeat suppression/minimum dwell policy so tips do not rapidly flip between states.
- [x] 3.3 Ensure tip updates remain local-only and do not alter textarea autosize or typing responsiveness.

## 4. Verification

- [x] 4.1 Validate in dev that sparse prompts surface one high-value tip and richer prompts advance to next missing detail.
- [x] 4.2 Validate no network calls are generated by tip analysis/update during typing.
- [x] 4.3 Update change task checklist status and capture any follow-up tuning items discovered during testing.
